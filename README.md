# 心理臨床学科 コース適性診断テスト

Renderの無料Web Serviceへ配置するためのファイルです。

- Build Command: `pip install -r requirements.txt`
- Start Command: `python app.py`
- Instance Type: `Free`

`app.py` は環境変数 `PORT` を読み、`0.0.0.0` で待ち受けます。
